.worktrees/
.workbuddy/
.astock-cache/
.tools/
node_modules/
dist/
release-assets/
项目说明书.md
应用创新类项目报告.md
演示文档操作提醒.md
src-tauri/target/
src-tauri/gen/
src-tauri/bin/
.pyinstaller/
__pycache__/
.pytest_cache/
.tmp-test/
.tmp/
.tmp_*
.tmp-*
.reference/
backend/astock_backtester.egg-info/
运行产物/

# 内部文档与截图：仅在本地留存，不入库、不上传
docs/

# Test coverage artifacts
.coverage
coverage.xml
htmlcov/
frontend/coverage/

# Vitest/Vite 在加载 TS 配置时落下的临时转译产物（每跑一次门禁必现）
frontend/vitest.config.ts.timestamp-*.mjs
frontend/vite.config.ts.timestamp-*.mjs

# 本地 agent 配置与工具缓存
.zcode/
.claude/
.ruff_cache/
.venv/
venv/
*.egg-info/

# 发布产物与日志兜底（当前只靠 release-assets/ 与 src-tauri/bin/ 两个目录规则兜底）
*.exe
*.msi
*.sig
*.log
latest.json

# 上面 *.log 是发布产物语境的兜底，风险是"测试 fixture 也用 .log 扩展名时被静默忽略"：
# git status 看不到、容易当成没提交的文件漏掉。对策是显式放行测试树（放在 *.log 之后
# 才生效，gitignore 后者覆盖前者）；当前 tests/ 下没有 .log 文件，这条是防御性规则，
# 不要删——删掉后将来新增的 .log fixture 会变成"测试跑得过、git 却看不见"的隐形文件。
# 守卫：tests/test_scripts.py::test_gitignore_keeps_test_log_negation_rule
!tests/**/*.log

# ===== ↑ 以上同步自 .gitignore（「从 .gitignore 同步」只重写以上部分）=====
.git/
.hg/
.svn/
bower_components/
jspm_packages/
site-packages/
coverage/
lcov-report/
cmakefiles/
cmake-build-*/
bazel-*/
pods/
deriveddata/
storybook-static/
playwright-report/
test-results/
allure-results/
allure-report/
cdk.out/
*.dist-info/
eggs/
pip-wheel-metadata/
wheels/
# ----- ↑ 以上为 ZCode 默认排除规则（自定义规则请写在本行下方，不会被同步/恢复改动）-----
# 自定义规则写在下方（本行提示可删除）
